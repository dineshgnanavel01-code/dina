import { useState } from 'react'
import { Link, useSearchParams } from 'react-router-dom'
import Button from '../components/Button'

export default function Auth() { const [params] = useSearchParams(); const [signup, setSignup] = useState(params.get('mode') === 'signup'); const [done, setDone] = useState(false); return <main className="min-h-[70vh] bg-brand-50 px-5 py-14"><div className="mx-auto max-w-md rounded-2xl border border-stone-200 bg-white p-7 shadow-xl shadow-stone-900/5 sm:p-9"><div className="text-center"><Link to="/" className="text-2xl font-bold text-brand-700">Dina Living<span className="text-brand-500">.</span></Link><h1 className="mt-8 text-2xl font-bold">{signup ? 'Create your account' : 'Welcome back'}</h1><p className="mt-2 text-sm text-stone-500">{signup ? 'Save favourites and manage your stays.' : 'Sign in to continue your rental journey.'}</p></div><form onSubmit={e => { e.preventDefault(); setDone(true) }} className="mt-7 space-y-4">{signup && <label className="block text-sm font-semibold">Full name<input required className="mt-2 w-full rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-brand-500"/></label>}<label className="block text-sm font-semibold">Email<input required type="email" className="mt-2 w-full rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-brand-500"/></label><label className="block text-sm font-semibold">Password<input required type="password" minLength="6" className="mt-2 w-full rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-brand-500"/></label><Button className="w-full">{signup ? 'Create account' : 'Log in'}</Button></form>{done && <p className="mt-4 rounded-xl bg-brand-50 p-3 text-center text-sm text-brand-700">Demo authentication complete.</p>}<p className="mt-6 text-center text-sm text-stone-500">{signup ? 'Already have an account?' : 'New to Dina Living?'} <button onClick={() => setSignup(!signup)} className="font-semibold text-brand-600">{signup ? 'Log in' : 'Sign up'}</button></p></div></main> }
