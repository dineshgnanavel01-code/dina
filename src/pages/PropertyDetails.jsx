import { useState } from 'react'
import { ArrowLeft, BedDouble, Check, Heart, MapPin, Star, Wifi, Car, Dumbbell, CalendarCheck } from 'lucide-react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { properties } from '../data/properties'
import Button from '../components/Button'

const iconFor = name => name === 'WiFi' ? Wifi : name === 'Parking' ? Car : name === 'Gym' ? Dumbbell : Check

export default function PropertyDetails() {
  const { id } = useParams(); const navigate = useNavigate(); const property = properties.find(p => p.id === Number(id)); const [selected, setSelected] = useState(0); const [booked, setBooked] = useState(false)
  if (!property) return <main className="mx-auto max-w-3xl px-5 py-24 text-center"><h1 className="text-3xl font-bold">Property not found</h1><Link to="/properties" className="mt-5 inline-block text-brand-600">Back to properties</Link></main>
  const gallery = [property.image, 'https://images.unsplash.com/photo-1600566753086-00f18fb6b3ea?auto=format&fit=crop&w=1200&q=80', 'https://images.unsplash.com/photo-1600210492486-724fe5c67fb0?auto=format&fit=crop&w=1200&q=80', 'https://images.unsplash.com/photo-1600585152915-d208bec867a1?auto=format&fit=crop&w=1200&q=80']
  return <main className="mx-auto max-w-7xl px-5 py-8 lg:px-8 lg:py-12"><button onClick={() => navigate(-1)} className="mb-6 flex items-center gap-2 text-sm font-semibold text-stone-500 hover:text-brand-600"><ArrowLeft size={16}/> Back to homes</button><div className="grid gap-3 md:grid-cols-4 md:grid-rows-2 md:h-[530px]"><div className="overflow-hidden rounded-2xl md:col-span-2 md:row-span-2"><img src={gallery[selected]} alt={property.title} className="h-full w-full object-cover"/></div>{gallery.slice(1).map((img, i) => <button key={img} onClick={() => setSelected(i + 1)} className="hidden overflow-hidden rounded-2xl md:block"><img src={img} alt="Property detail" className="h-full w-full object-cover transition hover:scale-105"/></button>)}</div><div className="mt-10 grid gap-12 lg:grid-cols-[1fr_350px]"><div><div className="flex flex-wrap items-start justify-between gap-5"><div><p className="text-sm font-semibold text-brand-600">{property.type}</p><h1 className="mt-1 text-4xl font-bold tracking-tight text-ink">{property.title}</h1><p className="mt-3 flex items-center gap-2 text-stone-500"><MapPin size={17}/>{property.location}</p></div><button className="grid h-11 w-11 place-items-center rounded-full border border-stone-200 hover:text-red-500"><Heart size={19}/></button></div><div className="mt-7 flex flex-wrap gap-6 border-y border-stone-200 py-5 text-sm text-stone-600"><span className="flex items-center gap-2"><BedDouble size={17}/> {property.bedrooms} bedrooms</span><span className="flex items-center gap-2"><Star size={17} className="text-amber-500" fill="currentColor"/> {property.rating} ({property.reviews} reviews)</span><span>Available now</span></div><section className="mt-8"><h2 className="text-xl font-bold">About this home</h2><p className="mt-3 max-w-2xl leading-7 text-stone-500">{property.description} Enjoy a thoughtfully considered space with quality finishes, practical storage and a comfortable setting for work, rest and everything in between.</p></section><section className="mt-8"><h2 className="text-xl font-bold">Amenities</h2><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-3">{property.amenities.map(a => { const Icon = iconFor(a); return <div key={a} className="flex items-center gap-3 rounded-xl bg-brand-50 px-4 py-3 text-sm font-medium"><Icon size={17} className="text-brand-600"/>{a}</div>})}</div></section></div><aside className="h-fit rounded-2xl border border-stone-200 bg-white p-6 shadow-xl shadow-stone-900/5 lg:sticky lg:top-28"><p className="text-sm text-stone-500">Monthly rent</p><p className="mt-1 text-3xl font-bold">£{property.price.toLocaleString()}<span className="text-sm font-normal text-stone-400"> / month</span></p><div className="mt-6 space-y-3"><label className="block text-xs font-semibold text-stone-500">Move-in date<input type="date" className="mt-1.5 w-full rounded-xl border border-stone-200 px-3 py-3 text-sm outline-none focus:border-brand-500"/></label><Button onClick={() => setBooked(true)} className="mt-2 w-full">{booked ? 'Request sent ✓' : 'Contact / Book now'}</Button></div>{booked && <p className="mt-4 rounded-xl bg-brand-50 p-3 text-xs leading-5 text-brand-700">Thanks! A Flex Living host will contact you shortly to confirm availability.</p>}<p className="mt-4 flex items-center gap-2 text-xs text-stone-400"><CalendarCheck size={15}/> Free cancellation on selected stays</p></aside></div></main>
}
